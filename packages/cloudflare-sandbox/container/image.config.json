{
  "$comment": [
    "Every version this image depends on, pinned in one place.",
    "sandbox.sdkVersion MUST equal the @cloudflare/sandbox version the Worker package depends on:",
    "the container server inside the base image and the SDK in the Worker speak a versioned protocol,",
    "and a mismatch shows up as an opaque exec failure at runtime rather than a build error.",
    "imageDigest pins the exact base manifest; imageRef is the human-readable equivalent."
  ],
  "sandbox": {
    "sdkVersion": "0.12.9",
    "imageRef": "docker.io/cloudflare/sandbox:0.12.9",
    "imageDigest": "sha256:4a56a37a3cfd9b38d65bb4b5d0b341e6490a3a4c0226274ae4c1cca4948e85fe",
    "platform": "linux/amd64",
    "baseDistro": "ubuntu:22.04",
    "entrypoint": "/container-server/sandbox"
  },
  "node": {
    "version": "24.21.0",
    "tarballSha256": "fd8e59d5a511510f6a298afb548f18c7d2b1be404d8b4a27d94fbe49f56cb2d6",
    "installPrefix": "/opt/nimbalyst/node"
  },
  "app": {
    "prefix": "/opt/nimbalyst/app",
    "dataDir": "/var/lib/nimbalyst",
    "workspaceDir": "/workspace",
    "user": "nimbalyst",
    "uid": 10001
  },
  "imageName": "nimbalyst/sandbox-node"
}
